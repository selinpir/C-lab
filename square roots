int main()
{
	float x;
	do
	{
		printf("Enter a positive number\n");
		scanf_s("%f",&x);
		if (x < 0)
		{
			printf("please enter a positive number\n");
		}
		if (x < 0) continue;
		printf("square root of your number : %f\n",sqrt(x));
	} 
	while (x);
	return 0;
}
